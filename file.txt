function dateTimeNow() {
                        const dateTimeOptions = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric', hour: 'numeric', minute: 'numeric' };
                        const timeOptions = { hour: 'numeric', minute: 'numeric' };
                        const dateOnlyOptions = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
                        const dateTimeNow = new Date().toLocaleDateString('en-US', dateTimeOptions);
                        const dateOnlyNow = new Date().toLocaleDateString('en-US', dateOnlyOptions);
                        const timeOnlyNow = new Date().toLocaleDateString('en-US', timeOptions);
                        const dateOnlyMonth = new Date().toLocaleDateString('en-US', { month: 'long' });
                        const dateDayMonthOnly = new Date().toLocaleDateString('en-US', { month: 'long', day: 'numeric' });
                        const yearOnly = new Date().toLocaleDateString('en-US', {  year: 'numeric' });
                        return { dateTimeNow, dateOnlyNow, dateOnlyMonth, dateDayMonthOnly, yearOnly,timeOnlyNow };
                    }



"Wednesday, March 1, 2023 at 10:46 AM"


function splitAt(text) {
  if (text.includes(' at ')) {
    const [datePart, timePart] = text.split(' at ');
    return { Date: datePart, time: timePart };
  }
  return null;
}

const input = "Wednesday, March 1, 2023 at 10:46 AM";
const result = splitAt(input);
console.log(result); // { Date: "Wednesday, March 1, 2023", time: "10:46 AM" }








Thank you for reaching out to us regarding the review and recreation of the credit card benefits page on our public website.

At this time, our team is occupied with the critical clean up and maintenance activity of the help and support section of the website. This activity is our top priority, and we are working hard to ensure that the content is accurate and usable for our customers.

Once this activity is completed, we will be able to focus our attention on your request. We understand the importance of the credit card benefits page, and we are currently researching the best way to recreate and represent its content on the website. We will ensure that it aligns with your team's goals and expectations.

We appreciate your patience and understanding as we work through our current workload. We will provide you with further updates as soon as we have them.

